/**
 * Auto generated test cases for "Oracle Integration API"
 *
 * NOTE: This class is auto generated by OracleSDKTestGenerator.
 * Do not edit the class manually.
 *
 * Copyright (c) 2020, Oracle and/or its affiliates.  All rights reserved.
 * This software is dual-licensed to you under the Universal Permissive License (UPL) 1.0 as shown at https://oss.oracle.com/licenses/upl or Apache License 2.0 as shown at http://www.apache.org/licenses/LICENSE-2.0. You may choose either license.
 */

import integration = require("../lib/integration/");
import TestingServiceClient = require("./testing-service-client");

import { expect } from "chai";
import common = require("oci-common");

describe("Test IntegrationInstance Api", () => {
  let tsclient: TestingServiceClient.TestingServiceClient;

  before(() => {
    tsclient = new TestingServiceClient.TestingServiceClient();
    return tsclient.startSession();
  });

  after(() => {
    return tsclient.endSession();
  });

  it("Test changeIntegrationInstanceCompartment method", async function() {
    if (!(await tsclient.isApiEnabled("integration", "ChangeIntegrationInstanceCompartment"))) {
      this.skip();
    }

    const config: any = await tsclient.getTestingConfig(
      "integration",
      "IntegrationInstance",
      "ChangeIntegrationInstanceCompartment"
    );

    const authenticationDetailProvider: common.AuthenticationDetailsProvider = new common.SimpleAuthenticationDetailsProvider(
      config.tenantId,
      config.userId,
      config.fingerprint,
      config.keyFileContent,
      config.passPhrase
    );

    const integrationClient = new integration.IntegrationInstanceClient({
      authenticationDetailsProvider: authenticationDetailProvider
    });
    if (config.endpoint) {
      integrationClient.endpoint = config.endpoint;
    } else {
      integrationClient.regionId = config.region;
    }
    const requests: any[] = await tsclient.getRequests(
      "integration",
      "ChangeIntegrationInstanceCompartment"
    );

    for (const data of requests) {
      try {
        console.debug("Request to OCI:", data.request);
        const response = await integrationClient.changeIntegrationInstanceCompartment(data.request);
        console.debug("Response from OCI:", response);

        const message: string = await tsclient.validateResponse(
          data.containerId,
          "integration",
          "ChangeIntegrationInstanceCompartmentRequest",
          data.request,
          "ChangeIntegrationInstanceCompartmentResponse",
          response
        );
        expect(message).to.equal("");
      } catch (err) {
        if (err instanceof common.OciError) {
          const message: string = await tsclient.validateError(
            data.containerId,
            "integration",
            "ChangeIntegrationInstanceCompartmentRequest",
            data.request,
            err
          );
          expect(message).to.equal("");
        } else {
          console.debug(err);
          throw err;
        }
      }
    }
  });

  it("Test createIntegrationInstance method", async function() {
    if (!(await tsclient.isApiEnabled("integration", "CreateIntegrationInstance"))) {
      this.skip();
    }

    const config: any = await tsclient.getTestingConfig(
      "integration",
      "IntegrationInstance",
      "CreateIntegrationInstance"
    );

    const authenticationDetailProvider: common.AuthenticationDetailsProvider = new common.SimpleAuthenticationDetailsProvider(
      config.tenantId,
      config.userId,
      config.fingerprint,
      config.keyFileContent,
      config.passPhrase
    );

    const integrationClient = new integration.IntegrationInstanceClient({
      authenticationDetailsProvider: authenticationDetailProvider
    });
    if (config.endpoint) {
      integrationClient.endpoint = config.endpoint;
    } else {
      integrationClient.regionId = config.region;
    }
    const requests: any[] = await tsclient.getRequests("integration", "CreateIntegrationInstance");

    for (const data of requests) {
      try {
        console.debug("Request to OCI:", data.request);
        const response = await integrationClient.createIntegrationInstance(data.request);
        console.debug("Response from OCI:", response);

        const message: string = await tsclient.validateResponse(
          data.containerId,
          "integration",
          "CreateIntegrationInstanceRequest",
          data.request,
          "CreateIntegrationInstanceResponse",
          response
        );
        expect(message).to.equal("");
      } catch (err) {
        if (err instanceof common.OciError) {
          const message: string = await tsclient.validateError(
            data.containerId,
            "integration",
            "CreateIntegrationInstanceRequest",
            data.request,
            err
          );
          expect(message).to.equal("");
        } else {
          console.debug(err);
          throw err;
        }
      }
    }
  });

  it("Test deleteIntegrationInstance method", async function() {
    if (!(await tsclient.isApiEnabled("integration", "DeleteIntegrationInstance"))) {
      this.skip();
    }

    const config: any = await tsclient.getTestingConfig(
      "integration",
      "IntegrationInstance",
      "DeleteIntegrationInstance"
    );

    const authenticationDetailProvider: common.AuthenticationDetailsProvider = new common.SimpleAuthenticationDetailsProvider(
      config.tenantId,
      config.userId,
      config.fingerprint,
      config.keyFileContent,
      config.passPhrase
    );

    const integrationClient = new integration.IntegrationInstanceClient({
      authenticationDetailsProvider: authenticationDetailProvider
    });
    if (config.endpoint) {
      integrationClient.endpoint = config.endpoint;
    } else {
      integrationClient.regionId = config.region;
    }
    const requests: any[] = await tsclient.getRequests("integration", "DeleteIntegrationInstance");

    for (const data of requests) {
      try {
        console.debug("Request to OCI:", data.request);
        const response = await integrationClient.deleteIntegrationInstance(data.request);
        console.debug("Response from OCI:", response);

        const message: string = await tsclient.validateResponse(
          data.containerId,
          "integration",
          "DeleteIntegrationInstanceRequest",
          data.request,
          "DeleteIntegrationInstanceResponse",
          response
        );
        expect(message).to.equal("");
      } catch (err) {
        if (err instanceof common.OciError) {
          const message: string = await tsclient.validateError(
            data.containerId,
            "integration",
            "DeleteIntegrationInstanceRequest",
            data.request,
            err
          );
          expect(message).to.equal("");
        } else {
          console.debug(err);
          throw err;
        }
      }
    }
  });

  it("Test getIntegrationInstance method", async function() {
    if (!(await tsclient.isApiEnabled("integration", "GetIntegrationInstance"))) {
      this.skip();
    }

    const config: any = await tsclient.getTestingConfig(
      "integration",
      "IntegrationInstance",
      "GetIntegrationInstance"
    );

    const authenticationDetailProvider: common.AuthenticationDetailsProvider = new common.SimpleAuthenticationDetailsProvider(
      config.tenantId,
      config.userId,
      config.fingerprint,
      config.keyFileContent,
      config.passPhrase
    );

    const integrationClient = new integration.IntegrationInstanceClient({
      authenticationDetailsProvider: authenticationDetailProvider
    });
    if (config.endpoint) {
      integrationClient.endpoint = config.endpoint;
    } else {
      integrationClient.regionId = config.region;
    }
    const requests: any[] = await tsclient.getRequests("integration", "GetIntegrationInstance");

    for (const data of requests) {
      try {
        console.debug("Request to OCI:", data.request);
        const response = await integrationClient.getIntegrationInstance(data.request);
        console.debug("Response from OCI:", response);

        const message: string = await tsclient.validateResponse(
          data.containerId,
          "integration",
          "GetIntegrationInstanceRequest",
          data.request,
          "GetIntegrationInstanceResponse",
          response
        );
        expect(message).to.equal("");
      } catch (err) {
        if (err instanceof common.OciError) {
          const message: string = await tsclient.validateError(
            data.containerId,
            "integration",
            "GetIntegrationInstanceRequest",
            data.request,
            err
          );
          expect(message).to.equal("");
        } else {
          console.debug(err);
          throw err;
        }
      }
    }
  });

  it("Test getWorkRequest method", async function() {
    if (!(await tsclient.isApiEnabled("integration", "GetWorkRequest"))) {
      this.skip();
    }

    const config: any = await tsclient.getTestingConfig(
      "integration",
      "IntegrationInstance",
      "GetWorkRequest"
    );

    const authenticationDetailProvider: common.AuthenticationDetailsProvider = new common.SimpleAuthenticationDetailsProvider(
      config.tenantId,
      config.userId,
      config.fingerprint,
      config.keyFileContent,
      config.passPhrase
    );

    const integrationClient = new integration.IntegrationInstanceClient({
      authenticationDetailsProvider: authenticationDetailProvider
    });
    if (config.endpoint) {
      integrationClient.endpoint = config.endpoint;
    } else {
      integrationClient.regionId = config.region;
    }
    const requests: any[] = await tsclient.getRequests("integration", "GetWorkRequest");

    for (const data of requests) {
      try {
        console.debug("Request to OCI:", data.request);
        const response = await integrationClient.getWorkRequest(data.request);
        console.debug("Response from OCI:", response);

        const message: string = await tsclient.validateResponse(
          data.containerId,
          "integration",
          "GetWorkRequestRequest",
          data.request,
          "GetWorkRequestResponse",
          response
        );
        expect(message).to.equal("");
      } catch (err) {
        if (err instanceof common.OciError) {
          const message: string = await tsclient.validateError(
            data.containerId,
            "integration",
            "GetWorkRequestRequest",
            data.request,
            err
          );
          expect(message).to.equal("");
        } else {
          console.debug(err);
          throw err;
        }
      }
    }
  });

  it("Test listIntegrationInstances method", async function() {
    if (!(await tsclient.isApiEnabled("integration", "ListIntegrationInstances"))) {
      this.skip();
    }

    const config: any = await tsclient.getTestingConfig(
      "integration",
      "IntegrationInstance",
      "ListIntegrationInstances"
    );

    const authenticationDetailProvider: common.AuthenticationDetailsProvider = new common.SimpleAuthenticationDetailsProvider(
      config.tenantId,
      config.userId,
      config.fingerprint,
      config.keyFileContent,
      config.passPhrase
    );

    const integrationClient = new integration.IntegrationInstanceClient({
      authenticationDetailsProvider: authenticationDetailProvider
    });
    if (config.endpoint) {
      integrationClient.endpoint = config.endpoint;
    } else {
      integrationClient.regionId = config.region;
    }
    const requests: any[] = await tsclient.getRequests("integration", "ListIntegrationInstances");

    for (const data of requests) {
      try {
        console.debug("Request to OCI:", data.request);
        const response = await integrationClient.listIntegrationInstances(data.request);
        console.debug("Response from OCI:", response);

        const message: string = await tsclient.validateResponse(
          data.containerId,
          "integration",
          "ListIntegrationInstancesRequest",
          data.request,
          "ListIntegrationInstancesResponse",
          response
        );
        expect(message).to.equal("");
      } catch (err) {
        if (err instanceof common.OciError) {
          const message: string = await tsclient.validateError(
            data.containerId,
            "integration",
            "ListIntegrationInstancesRequest",
            data.request,
            err
          );
          expect(message).to.equal("");
        } else {
          console.debug(err);
          throw err;
        }
      }
    }
  });

  it("Test listWorkRequestErrors method", async function() {
    if (!(await tsclient.isApiEnabled("integration", "ListWorkRequestErrors"))) {
      this.skip();
    }

    const config: any = await tsclient.getTestingConfig(
      "integration",
      "IntegrationInstance",
      "ListWorkRequestErrors"
    );

    const authenticationDetailProvider: common.AuthenticationDetailsProvider = new common.SimpleAuthenticationDetailsProvider(
      config.tenantId,
      config.userId,
      config.fingerprint,
      config.keyFileContent,
      config.passPhrase
    );

    const integrationClient = new integration.IntegrationInstanceClient({
      authenticationDetailsProvider: authenticationDetailProvider
    });
    if (config.endpoint) {
      integrationClient.endpoint = config.endpoint;
    } else {
      integrationClient.regionId = config.region;
    }
    const requests: any[] = await tsclient.getRequests("integration", "ListWorkRequestErrors");

    for (const data of requests) {
      try {
        console.debug("Request to OCI:", data.request);
        const response = await integrationClient.listWorkRequestErrors(data.request);
        console.debug("Response from OCI:", response);

        const message: string = await tsclient.validateResponse(
          data.containerId,
          "integration",
          "ListWorkRequestErrorsRequest",
          data.request,
          "ListWorkRequestErrorsResponse",
          response
        );
        expect(message).to.equal("");
      } catch (err) {
        if (err instanceof common.OciError) {
          const message: string = await tsclient.validateError(
            data.containerId,
            "integration",
            "ListWorkRequestErrorsRequest",
            data.request,
            err
          );
          expect(message).to.equal("");
        } else {
          console.debug(err);
          throw err;
        }
      }
    }
  });

  it("Test listWorkRequestLogs method", async function() {
    if (!(await tsclient.isApiEnabled("integration", "ListWorkRequestLogs"))) {
      this.skip();
    }

    const config: any = await tsclient.getTestingConfig(
      "integration",
      "IntegrationInstance",
      "ListWorkRequestLogs"
    );

    const authenticationDetailProvider: common.AuthenticationDetailsProvider = new common.SimpleAuthenticationDetailsProvider(
      config.tenantId,
      config.userId,
      config.fingerprint,
      config.keyFileContent,
      config.passPhrase
    );

    const integrationClient = new integration.IntegrationInstanceClient({
      authenticationDetailsProvider: authenticationDetailProvider
    });
    if (config.endpoint) {
      integrationClient.endpoint = config.endpoint;
    } else {
      integrationClient.regionId = config.region;
    }
    const requests: any[] = await tsclient.getRequests("integration", "ListWorkRequestLogs");

    for (const data of requests) {
      try {
        console.debug("Request to OCI:", data.request);
        const response = await integrationClient.listWorkRequestLogs(data.request);
        console.debug("Response from OCI:", response);

        const message: string = await tsclient.validateResponse(
          data.containerId,
          "integration",
          "ListWorkRequestLogsRequest",
          data.request,
          "ListWorkRequestLogsResponse",
          response
        );
        expect(message).to.equal("");
      } catch (err) {
        if (err instanceof common.OciError) {
          const message: string = await tsclient.validateError(
            data.containerId,
            "integration",
            "ListWorkRequestLogsRequest",
            data.request,
            err
          );
          expect(message).to.equal("");
        } else {
          console.debug(err);
          throw err;
        }
      }
    }
  });

  it("Test listWorkRequests method", async function() {
    if (!(await tsclient.isApiEnabled("integration", "ListWorkRequests"))) {
      this.skip();
    }

    const config: any = await tsclient.getTestingConfig(
      "integration",
      "IntegrationInstance",
      "ListWorkRequests"
    );

    const authenticationDetailProvider: common.AuthenticationDetailsProvider = new common.SimpleAuthenticationDetailsProvider(
      config.tenantId,
      config.userId,
      config.fingerprint,
      config.keyFileContent,
      config.passPhrase
    );

    const integrationClient = new integration.IntegrationInstanceClient({
      authenticationDetailsProvider: authenticationDetailProvider
    });
    if (config.endpoint) {
      integrationClient.endpoint = config.endpoint;
    } else {
      integrationClient.regionId = config.region;
    }
    const requests: any[] = await tsclient.getRequests("integration", "ListWorkRequests");

    for (const data of requests) {
      try {
        console.debug("Request to OCI:", data.request);
        const response = await integrationClient.listWorkRequests(data.request);
        console.debug("Response from OCI:", response);

        const message: string = await tsclient.validateResponse(
          data.containerId,
          "integration",
          "ListWorkRequestsRequest",
          data.request,
          "ListWorkRequestsResponse",
          response
        );
        expect(message).to.equal("");
      } catch (err) {
        if (err instanceof common.OciError) {
          const message: string = await tsclient.validateError(
            data.containerId,
            "integration",
            "ListWorkRequestsRequest",
            data.request,
            err
          );
          expect(message).to.equal("");
        } else {
          console.debug(err);
          throw err;
        }
      }
    }
  });

  it("Test updateIntegrationInstance method", async function() {
    if (!(await tsclient.isApiEnabled("integration", "UpdateIntegrationInstance"))) {
      this.skip();
    }

    const config: any = await tsclient.getTestingConfig(
      "integration",
      "IntegrationInstance",
      "UpdateIntegrationInstance"
    );

    const authenticationDetailProvider: common.AuthenticationDetailsProvider = new common.SimpleAuthenticationDetailsProvider(
      config.tenantId,
      config.userId,
      config.fingerprint,
      config.keyFileContent,
      config.passPhrase
    );

    const integrationClient = new integration.IntegrationInstanceClient({
      authenticationDetailsProvider: authenticationDetailProvider
    });
    if (config.endpoint) {
      integrationClient.endpoint = config.endpoint;
    } else {
      integrationClient.regionId = config.region;
    }
    const requests: any[] = await tsclient.getRequests("integration", "UpdateIntegrationInstance");

    for (const data of requests) {
      try {
        console.debug("Request to OCI:", data.request);
        const response = await integrationClient.updateIntegrationInstance(data.request);
        console.debug("Response from OCI:", response);

        const message: string = await tsclient.validateResponse(
          data.containerId,
          "integration",
          "UpdateIntegrationInstanceRequest",
          data.request,
          "UpdateIntegrationInstanceResponse",
          response
        );
        expect(message).to.equal("");
      } catch (err) {
        if (err instanceof common.OciError) {
          const message: string = await tsclient.validateError(
            data.containerId,
            "integration",
            "UpdateIntegrationInstanceRequest",
            data.request,
            err
          );
          expect(message).to.equal("");
        } else {
          console.debug(err);
          throw err;
        }
      }
    }
  });
});
