/**
 * Auto generated test cases for "Search Service API"
 *
 * NOTE: This class is auto generated by OracleSDKTestGenerator.
 * Do not edit the class manually.
 *
 * Copyright (c) 2020, Oracle and/or its affiliates.  All rights reserved.
 * This software is dual-licensed to you under the Universal Permissive License (UPL) 1.0 as shown at https://oss.oracle.com/licenses/upl or Apache License 2.0 as shown at http://www.apache.org/licenses/LICENSE-2.0. You may choose either license.
 */

import resourcesearch = require("../lib/resourcesearch/");
import TestingServiceClient = require("./testing-service-client");

import { expect } from "chai";
import common = require("oci-common");

describe("Test ResourceSearch Api", () => {
  let tsclient: TestingServiceClient.TestingServiceClient;

  before(() => {
    tsclient = new TestingServiceClient.TestingServiceClient();
    return tsclient.startSession();
  });

  after(() => {
    return tsclient.endSession();
  });

  it("Test getResourceType method", async function() {
    if (!(await tsclient.isApiEnabled("resourcesearch", "GetResourceType"))) {
      this.skip();
    }

    const config: any = await tsclient.getTestingConfig(
      "resourcesearch",
      "ResourceSearch",
      "GetResourceType"
    );

    const authenticationDetailProvider: common.AuthenticationDetailsProvider = new common.SimpleAuthenticationDetailsProvider(
      config.tenantId,
      config.userId,
      config.fingerprint,
      config.keyFileContent,
      config.passPhrase
    );

    const resourcesearchClient = new resourcesearch.ResourceSearchClient({
      authenticationDetailsProvider: authenticationDetailProvider
    });
    if (config.endpoint) {
      resourcesearchClient.endpoint = config.endpoint;
    } else {
      resourcesearchClient.regionId = config.region;
    }
    const requests: any[] = await tsclient.getRequests("resourcesearch", "GetResourceType");

    for (const data of requests) {
      try {
        console.debug("Request to OCI:", data.request);
        const response = await resourcesearchClient.getResourceType(data.request);
        console.debug("Response from OCI:", response);

        const message: string = await tsclient.validateResponse(
          data.containerId,
          "resourcesearch",
          "GetResourceTypeRequest",
          data.request,
          "GetResourceTypeResponse",
          response
        );
        expect(message).to.equal("");
      } catch (err) {
        if (err instanceof common.OciError) {
          const message: string = await tsclient.validateError(
            data.containerId,
            "resourcesearch",
            "GetResourceTypeRequest",
            data.request,
            err
          );
          expect(message).to.equal("");
        } else {
          console.debug(err);
          throw err;
        }
      }
    }
  });

  it("Test listResourceTypes method", async function() {
    if (!(await tsclient.isApiEnabled("resourcesearch", "ListResourceTypes"))) {
      this.skip();
    }

    const config: any = await tsclient.getTestingConfig(
      "resourcesearch",
      "ResourceSearch",
      "ListResourceTypes"
    );

    const authenticationDetailProvider: common.AuthenticationDetailsProvider = new common.SimpleAuthenticationDetailsProvider(
      config.tenantId,
      config.userId,
      config.fingerprint,
      config.keyFileContent,
      config.passPhrase
    );

    const resourcesearchClient = new resourcesearch.ResourceSearchClient({
      authenticationDetailsProvider: authenticationDetailProvider
    });
    if (config.endpoint) {
      resourcesearchClient.endpoint = config.endpoint;
    } else {
      resourcesearchClient.regionId = config.region;
    }
    const requests: any[] = await tsclient.getRequests("resourcesearch", "ListResourceTypes");

    for (const data of requests) {
      try {
        console.debug("Request to OCI:", data.request);
        const response = await resourcesearchClient.listResourceTypes(data.request);
        console.debug("Response from OCI:", response);

        const message: string = await tsclient.validateResponse(
          data.containerId,
          "resourcesearch",
          "ListResourceTypesRequest",
          data.request,
          "ListResourceTypesResponse",
          response
        );
        expect(message).to.equal("");
      } catch (err) {
        if (err instanceof common.OciError) {
          const message: string = await tsclient.validateError(
            data.containerId,
            "resourcesearch",
            "ListResourceTypesRequest",
            data.request,
            err
          );
          expect(message).to.equal("");
        } else {
          console.debug(err);
          throw err;
        }
      }
    }
  });

  it("Test searchResources method", async function() {
    if (!(await tsclient.isApiEnabled("resourcesearch", "SearchResources"))) {
      this.skip();
    }

    const config: any = await tsclient.getTestingConfig(
      "resourcesearch",
      "ResourceSearch",
      "SearchResources"
    );

    const authenticationDetailProvider: common.AuthenticationDetailsProvider = new common.SimpleAuthenticationDetailsProvider(
      config.tenantId,
      config.userId,
      config.fingerprint,
      config.keyFileContent,
      config.passPhrase
    );

    const resourcesearchClient = new resourcesearch.ResourceSearchClient({
      authenticationDetailsProvider: authenticationDetailProvider
    });
    if (config.endpoint) {
      resourcesearchClient.endpoint = config.endpoint;
    } else {
      resourcesearchClient.regionId = config.region;
    }
    const requests: any[] = await tsclient.getRequests("resourcesearch", "SearchResources");

    for (const data of requests) {
      try {
        console.debug("Request to OCI:", data.request);
        const response = await resourcesearchClient.searchResources(data.request);
        console.debug("Response from OCI:", response);

        const message: string = await tsclient.validateResponse(
          data.containerId,
          "resourcesearch",
          "SearchResourcesRequest",
          data.request,
          "SearchResourcesResponse",
          response
        );
        expect(message).to.equal("");
      } catch (err) {
        if (err instanceof common.OciError) {
          const message: string = await tsclient.validateError(
            data.containerId,
            "resourcesearch",
            "SearchResourcesRequest",
            data.request,
            err
          );
          expect(message).to.equal("");
        } else {
          console.debug(err);
          throw err;
        }
      }
    }
  });
});
