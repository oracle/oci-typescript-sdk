/**
 * Auto generated test cases for "Audit API"
 *
 * NOTE: This class is auto generated by OracleSDKTestGenerator.
 * Do not edit the class manually.
 *
 * Copyright (c) 2020, Oracle and/or its affiliates.  All rights reserved.
 * This software is dual-licensed to you under the Universal Permissive License (UPL) 1.0 as shown at https://oss.oracle.com/licenses/upl or Apache License 2.0 as shown at http://www.apache.org/licenses/LICENSE-2.0. You may choose either license.
 */

import audit = require("../lib/audit/");
import TestingServiceClient = require("./testing-service-client");

import { expect } from "chai";
import common = require("oci-common");

describe("Test Audit Api", () => {
  let tsclient: TestingServiceClient.TestingServiceClient;

  before(() => {
    tsclient = new TestingServiceClient.TestingServiceClient();
    return tsclient.startSession();
  });

  after(() => {
    return tsclient.endSession();
  });

  it("Test getConfiguration method", async function() {
    if (!(await tsclient.isApiEnabled("audit", "GetConfiguration"))) {
      this.skip();
    }

    const config: any = await tsclient.getTestingConfig("audit", "Audit", "GetConfiguration");

    const authenticationDetailProvider: common.AuthenticationDetailsProvider = new common.SimpleAuthenticationDetailsProvider(
      config.tenantId,
      config.userId,
      config.fingerprint,
      config.keyFileContent,
      config.passPhrase
    );

    const auditClient = new audit.AuditClient({
      authenticationDetailsProvider: authenticationDetailProvider
    });
    if (config.endpoint) {
      auditClient.endpoint = config.endpoint;
    } else {
      auditClient.regionId = config.region;
    }
    const requests: any[] = await tsclient.getRequests("audit", "GetConfiguration");

    for (const data of requests) {
      try {
        console.debug("Request to OCI:", data.request);
        const response = await auditClient.getConfiguration(data.request);
        console.debug("Response from OCI:", response);

        const message: string = await tsclient.validateResponse(
          data.containerId,
          "audit",
          "GetConfigurationRequest",
          data.request,
          "GetConfigurationResponse",
          response
        );
        expect(message).to.equal("");
      } catch (err) {
        if (err instanceof common.OciError) {
          const message: string = await tsclient.validateError(
            data.containerId,
            "audit",
            "GetConfigurationRequest",
            data.request,
            err
          );
          expect(message).to.equal("");
        } else {
          console.debug(err);
          throw err;
        }
      }
    }
  });

  it("Test listEvents method", async function() {
    if (!(await tsclient.isApiEnabled("audit", "ListEvents"))) {
      this.skip();
    }

    const config: any = await tsclient.getTestingConfig("audit", "Audit", "ListEvents");

    const authenticationDetailProvider: common.AuthenticationDetailsProvider = new common.SimpleAuthenticationDetailsProvider(
      config.tenantId,
      config.userId,
      config.fingerprint,
      config.keyFileContent,
      config.passPhrase
    );

    const auditClient = new audit.AuditClient({
      authenticationDetailsProvider: authenticationDetailProvider
    });
    if (config.endpoint) {
      auditClient.endpoint = config.endpoint;
    } else {
      auditClient.regionId = config.region;
    }
    const requests: any[] = await tsclient.getRequests("audit", "ListEvents");

    for (const data of requests) {
      try {
        console.debug("Request to OCI:", data.request);
        const response = await auditClient.listEvents(data.request);
        console.debug("Response from OCI:", response);

        const message: string = await tsclient.validateResponse(
          data.containerId,
          "audit",
          "ListEventsRequest",
          data.request,
          "ListEventsResponse",
          response
        );
        expect(message).to.equal("");
      } catch (err) {
        if (err instanceof common.OciError) {
          const message: string = await tsclient.validateError(
            data.containerId,
            "audit",
            "ListEventsRequest",
            data.request,
            err
          );
          expect(message).to.equal("");
        } else {
          console.debug(err);
          throw err;
        }
      }
    }
  });

  it("Test updateConfiguration method", async function() {
    if (!(await tsclient.isApiEnabled("audit", "UpdateConfiguration"))) {
      this.skip();
    }

    const config: any = await tsclient.getTestingConfig("audit", "Audit", "UpdateConfiguration");

    const authenticationDetailProvider: common.AuthenticationDetailsProvider = new common.SimpleAuthenticationDetailsProvider(
      config.tenantId,
      config.userId,
      config.fingerprint,
      config.keyFileContent,
      config.passPhrase
    );

    const auditClient = new audit.AuditClient({
      authenticationDetailsProvider: authenticationDetailProvider
    });
    if (config.endpoint) {
      auditClient.endpoint = config.endpoint;
    } else {
      auditClient.regionId = config.region;
    }
    const requests: any[] = await tsclient.getRequests("audit", "UpdateConfiguration");

    for (const data of requests) {
      try {
        console.debug("Request to OCI:", data.request);
        const response = await auditClient.updateConfiguration(data.request);
        console.debug("Response from OCI:", response);

        const message: string = await tsclient.validateResponse(
          data.containerId,
          "audit",
          "UpdateConfigurationRequest",
          data.request,
          "UpdateConfigurationResponse",
          response
        );
        expect(message).to.equal("");
      } catch (err) {
        if (err instanceof common.OciError) {
          const message: string = await tsclient.validateError(
            data.containerId,
            "audit",
            "UpdateConfigurationRequest",
            data.request,
            err
          );
          expect(message).to.equal("");
        } else {
          console.debug(err);
          throw err;
        }
      }
    }
  });
});
