/**
 * Batch API
 * Use the Batch Control Plane API to encapsulate and manage all aspects of computationally intensive jobs.
 * OpenAPI spec version: 20251031
 *
 *
 * NOTE: This class is auto generated by OracleSDKGenerator.
 * Do not edit the class manually.
 *
 * Copyright (c) 2020, 2026, Oracle and/or its affiliates.  All rights reserved.
 * This software is dual-licensed to you under the Universal Permissive License (UPL) 1.0 as shown at https://oss.oracle.com/licenses/upl or Apache License 2.0 as shown at http://www.apache.org/licenses/LICENSE-2.0. You may choose either license.
 */

import * as model from "../model";
import common = require("oci-common");

/**
 * A batch task contains common properties for all types of job tasks.
 */
export interface BatchTask {
  /**
   * The UUID of batch task.
   */
  "id": string;
  /**
   * The name of the batch task. It must be unique within its parent batch job.
   */
  "name": string;
  /**
   * An optional description that provides additional context next to the displayName.
   */
  "description"?: string;
  /**
   * The current state of the batch task.
   */
  "lifecycleState"?: BatchTask.LifecycleState;
  /**
   * A message that describes the current state of the batch task in more detail. For example, can be used to provide actionable information for a resource in the Failed state.
   *
   */
  "lifecycleDetails"?: string;
  /**
   * A list of resources (for example licences) this task needs for its execution.
   */
  "entitlementClaims": Array<string>;
  /**
   * A list of tasks from the same job this task depends on referenced by name.
   */
  "dependencies": Array<string>;
  /**
   * Environment variables to use for the task execution.
   */
  "environmentVariables": Array<model.EnvironmentVariable>;

  "type": string;
}

export namespace BatchTask {
  export enum LifecycleState {
    Accepted = "ACCEPTED",
    Waiting = "WAITING",
    InProgress = "IN_PROGRESS",
    Succeeded = "SUCCEEDED",
    Failed = "FAILED",
    Canceling = "CANCELING",
    Canceled = "CANCELED",
    /**
     * This value is used if a service returns a value for this enum that is not recognized by this
     * version of the SDK.
     */
    UnknownValue = "UNKNOWN_VALUE"
  }

  export function getJsonObj(obj: BatchTask): object {
    const jsonObj = {
      ...obj,
      ...{
        "environmentVariables": obj.environmentVariables
          ? obj.environmentVariables.map(item => {
              return model.EnvironmentVariable.getJsonObj(item);
            })
          : undefined
      }
    };

    if (obj && "type" in obj && obj.type) {
      switch (obj.type) {
        case "COMPUTE":
          return model.ComputeTask.getJsonObj(<model.ComputeTask>(<object>jsonObj), true);
        default:
          if (common.LOG.logger) common.LOG.logger.info(`Unknown value for: ${obj.type}`);
      }
    }
    return jsonObj;
  }
  export function getDeserializedJsonObj(obj: BatchTask): object {
    const jsonObj = {
      ...obj,
      ...{
        "environmentVariables": obj.environmentVariables
          ? obj.environmentVariables.map(item => {
              return model.EnvironmentVariable.getDeserializedJsonObj(item);
            })
          : undefined
      }
    };

    if (obj && "type" in obj && obj.type) {
      switch (obj.type) {
        case "COMPUTE":
          return model.ComputeTask.getDeserializedJsonObj(
            <model.ComputeTask>(<object>jsonObj),
            true
          );
        default:
          if (common.LOG.logger) common.LOG.logger.info(`Unknown value for: ${obj.type}`);
      }
    }
    return jsonObj;
  }
}
