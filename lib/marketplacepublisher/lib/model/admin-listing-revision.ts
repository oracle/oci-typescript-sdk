/**
 * MarketplacePublisherService API
 * Use the Marketplace Publisher API to manage the publishing of applications in Oracle Cloud Infrastructure Marketplace.
 * OpenAPI spec version: 20241201
 *
 *
 * NOTE: This class is auto generated by OracleSDKGenerator.
 * Do not edit the class manually.
 *
 * Copyright (c) 2020, 2026, Oracle and/or its affiliates.  All rights reserved.
 * This software is dual-licensed to you under the Universal Permissive License (UPL) 1.0 as shown at https://oss.oracle.com/licenses/upl or Apache License 2.0 as shown at http://www.apache.org/licenses/LICENSE-2.0. You may choose either license.
 */

import * as model from "../model";
import common = require("oci-common");

/**
 * The model for an Oracle Cloud Infrastructure Marketplace Publisher listing revision.
 */
export interface AdminListingRevision {
  /**
   * The OCID for the listing revision in Marketplace Publisher.
   */
  "id": string;
  /**
   * The unique legacy identifier for the listing.
   */
  "legacyId"?: string;
  /**
   * The unique identifier for the listing this revision belongs to.
   */
  "listingId": string;
  /**
   * The unique identifier for the compartment.
   */
  "compartmentId"?: string;
  /**
   * The OCID for the publisher in Marketplace Publisher.
   */
  "publisherId": string;
  /**
   * The user OCID of the administrator who approved the listing revision.
   */
  "approverId"?: string;
  /**
   * The email address of the administrator who approved the listing revision.
   */
  "approverEmail"?: string;
  /**
   * The name for the listing revision.
   */
  "displayName": string;
  /**
   * The revision number of the listing revision. This is an internal attribute
   */
  "revisionNumber"?: string;
  /**
   * Single line introduction for the listing revision.
   */
  "headline": string;
  /**
   * The tagline for the listing revision.
   */
  "tagline"?: string;
  /**
   * Keywords associated with the listing revision.
   */
  "keywords"?: string;
  /**
   * A short description of the listing revision.
   */
  "shortDescription"?: string;
  /**
   * Usage information for the listing revision.
   */
  "usageInformation"?: string;
  /**
   * A long description for the listing revision.
   */
  "longDescription"?: string;
  /**
   * The time the listing revision was created. An RFC3339 formatted datetime string.
   */
  "timeCreated": Date;
  /**
   * The time the listing revision was updated. An RFC3339 formatted datetime string.
   */
  "timeUpdated": Date;
  "contentLanguage"?: model.LanguageItem;
  /**
   * Languages supported by the publisher for the listing revision.
   */
  "supportedlanguages"?: Array<model.LanguageItem>;
  /**
   * Contact information to use to get support from the publisher for the listing revision.
   */
  "supportContacts"?: Array<model.SupportContact>;
  /**
   * Links to support resources for the listing revision.
   */
  "supportLinks"?: Array<model.NamedLink>;
  "icon"?: model.ListingRevisionIconAttachment;
  "banner"?: model.ListingRevisionBannerAttachment;
  /**
   * The current status for the listing revision.
   */
  "status": string;
  /**
   * Status notes for the listing revision.
   */
  "statusNotes"?: string;
  /**
   * The current state of the listing revision.
   */
  "lifecycleState": string;
  /**
   * The listing's package type. Populated from the listing.
   */
  "packageType": model.PackageType;
  /**
    * Additional metadata key/value pairs for the listing revision.
* <p>
For example:
* <p>
{@code {\"oracleStandardTermVersionId\": \"1234\"}}
* 
    */
  "extendedMetadata"?: { [key: string]: string };
  /**
   * Simple key-value pair that is applied without any predefined name, type or scope. Exists for cross-compatibility only.
   * Example: {@code {\"bar-key\": \"value\"}}
   *
   */
  "freeformTags"?: { [key: string]: string };
  /**
   * Defined tags for this resource. Each key is predefined and scoped to a namespace.
   * Example: {@code {\"foo-namespace\": {\"bar-key\": \"value\"}}}
   *
   */
  "definedTags"?: { [key: string]: { [key: string]: any } };
  /**
   * System tags for this resource. Each key is predefined and scoped to a namespace.
   * Example: {@code {\"orcl-cloud\": {\"free-tier-retained\": \"true\"}}}
   *
   */
  "systemTags"?: { [key: string]: { [key: string]: any } };

  "listingType": string;
}

export namespace AdminListingRevision {
  export function getJsonObj(obj: AdminListingRevision): object {
    const jsonObj = {
      ...obj,
      ...{
        "contentLanguage": obj.contentLanguage
          ? model.LanguageItem.getJsonObj(obj.contentLanguage)
          : undefined,
        "supportedlanguages": obj.supportedlanguages
          ? obj.supportedlanguages.map(item => {
              return model.LanguageItem.getJsonObj(item);
            })
          : undefined,
        "supportContacts": obj.supportContacts
          ? obj.supportContacts.map(item => {
              return model.SupportContact.getJsonObj(item);
            })
          : undefined,
        "supportLinks": obj.supportLinks
          ? obj.supportLinks.map(item => {
              return model.NamedLink.getJsonObj(item);
            })
          : undefined,
        "icon": obj.icon ? model.ListingRevisionIconAttachment.getJsonObj(obj.icon) : undefined,
        "banner": obj.banner
          ? model.ListingRevisionBannerAttachment.getJsonObj(obj.banner)
          : undefined
      }
    };

    if (obj && "listingType" in obj && obj.listingType) {
      switch (obj.listingType) {
        case "SERVICE":
          return model.AdminServiceListingRevision.getJsonObj(
            <model.AdminServiceListingRevision>(<object>jsonObj),
            true
          );
        case "AI_AGENT":
          return model.AdminAiAgentListingRevision.getJsonObj(
            <model.AdminAiAgentListingRevision>(<object>jsonObj),
            true
          );
        case "OCI_APPLICATION":
          return model.AdminOciListingRevision.getJsonObj(
            <model.AdminOciListingRevision>(<object>jsonObj),
            true
          );
        case "LEAD_GENERATION":
          return model.AdminLeadGenListingRevision.getJsonObj(
            <model.AdminLeadGenListingRevision>(<object>jsonObj),
            true
          );
        default:
          if (common.LOG.logger) common.LOG.logger.info(`Unknown value for: ${obj.listingType}`);
      }
    }
    return jsonObj;
  }
  export function getDeserializedJsonObj(obj: AdminListingRevision): object {
    const jsonObj = {
      ...obj,
      ...{
        "contentLanguage": obj.contentLanguage
          ? model.LanguageItem.getDeserializedJsonObj(obj.contentLanguage)
          : undefined,
        "supportedlanguages": obj.supportedlanguages
          ? obj.supportedlanguages.map(item => {
              return model.LanguageItem.getDeserializedJsonObj(item);
            })
          : undefined,
        "supportContacts": obj.supportContacts
          ? obj.supportContacts.map(item => {
              return model.SupportContact.getDeserializedJsonObj(item);
            })
          : undefined,
        "supportLinks": obj.supportLinks
          ? obj.supportLinks.map(item => {
              return model.NamedLink.getDeserializedJsonObj(item);
            })
          : undefined,
        "icon": obj.icon
          ? model.ListingRevisionIconAttachment.getDeserializedJsonObj(obj.icon)
          : undefined,
        "banner": obj.banner
          ? model.ListingRevisionBannerAttachment.getDeserializedJsonObj(obj.banner)
          : undefined
      }
    };

    if (obj && "listingType" in obj && obj.listingType) {
      switch (obj.listingType) {
        case "SERVICE":
          return model.AdminServiceListingRevision.getDeserializedJsonObj(
            <model.AdminServiceListingRevision>(<object>jsonObj),
            true
          );
        case "AI_AGENT":
          return model.AdminAiAgentListingRevision.getDeserializedJsonObj(
            <model.AdminAiAgentListingRevision>(<object>jsonObj),
            true
          );
        case "OCI_APPLICATION":
          return model.AdminOciListingRevision.getDeserializedJsonObj(
            <model.AdminOciListingRevision>(<object>jsonObj),
            true
          );
        case "LEAD_GENERATION":
          return model.AdminLeadGenListingRevision.getDeserializedJsonObj(
            <model.AdminLeadGenListingRevision>(<object>jsonObj),
            true
          );
        default:
          if (common.LOG.logger) common.LOG.logger.info(`Unknown value for: ${obj.listingType}`);
      }
    }
    return jsonObj;
  }
}
