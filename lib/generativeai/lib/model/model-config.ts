/**
 * Generative AI Service Management API
 * OCI Generative AI is a fully managed service that provides a set of state-of-the-art, customizable large language models (LLMs) that cover a wide range of use cases for text generation, summarization, and text embeddings. 

Use the Generative AI service management API to create and manage {@link DedicatedAiCluster}, {@link Endpoint}, {@link Model}, and {@link WorkRequest} in the Generative AI service. For example, create a custom model by fine-tuning an out-of-the-box model using your own data, on a fine-tuning dedicated AI cluster. Then, create a hosting dedicated AI cluster with an endpoint to host your custom model. 

To access your custom model endpoints, or to try the out-of-the-box models to generate text, summarize, and create text embeddings see the [Generative AI Inference API](https://docs.oracle.com/iaas/api/#/en/generative-ai-inference/latest/).

To learn more about the service, see the [Generative AI documentation](https://docs.oracle.com/iaas/Content/generative-ai/home.htm).

 * OpenAPI spec version: 20231130
 * 
 *
 * NOTE: This class is auto generated by OracleSDKGenerator.
 * Do not edit the class manually.
 *
 * Copyright (c) 2020, 2026, Oracle and/or its affiliates.  All rights reserved.
 * This software is dual-licensed to you under the Universal Permissive License (UPL) 1.0 as shown at https://oss.oracle.com/licenses/upl or Apache License 2.0 as shown at http://www.apache.org/licenses/LICENSE-2.0. You may choose either license.
 */

import * as model from "../model";
import common = require("oci-common");

/**
 * Base model configuration shared across GenAI Project memory.
 */
export interface ModelConfig {
  "llmSelection": model.GenAiModelLlmSelection;

  "modelConfigType": string;
}

export namespace ModelConfig {
  export function getJsonObj(obj: ModelConfig): object {
    const jsonObj = {
      ...obj,
      ...{
        "llmSelection": obj.llmSelection
          ? model.LlmSelection.getJsonObj(obj.llmSelection)
          : undefined
      }
    };

    if (obj && "modelConfigType" in obj && obj.modelConfigType) {
      switch (obj.modelConfigType) {
        case "CONDENSER":
          return model.CondenserConfig.getJsonObj(<model.CondenserConfig>(<object>jsonObj), true);
        case "EXTRACTION":
          return model.ExtractionConfig.getJsonObj(<model.ExtractionConfig>(<object>jsonObj), true);
        case "EMBEDDING":
          return model.EmbeddingConfig.getJsonObj(<model.EmbeddingConfig>(<object>jsonObj), true);
        default:
          if (common.LOG.logger)
            common.LOG.logger.info(`Unknown value for: ${obj.modelConfigType}`);
      }
    }
    return jsonObj;
  }
  export function getDeserializedJsonObj(obj: ModelConfig): object {
    const jsonObj = {
      ...obj,
      ...{
        "llmSelection": obj.llmSelection
          ? model.LlmSelection.getDeserializedJsonObj(obj.llmSelection)
          : undefined
      }
    };

    if (obj && "modelConfigType" in obj && obj.modelConfigType) {
      switch (obj.modelConfigType) {
        case "CONDENSER":
          return model.CondenserConfig.getDeserializedJsonObj(
            <model.CondenserConfig>(<object>jsonObj),
            true
          );
        case "EXTRACTION":
          return model.ExtractionConfig.getDeserializedJsonObj(
            <model.ExtractionConfig>(<object>jsonObj),
            true
          );
        case "EMBEDDING":
          return model.EmbeddingConfig.getDeserializedJsonObj(
            <model.EmbeddingConfig>(<object>jsonObj),
            true
          );
        default:
          if (common.LOG.logger)
            common.LOG.logger.info(`Unknown value for: ${obj.modelConfigType}`);
      }
    }
    return jsonObj;
  }
}
