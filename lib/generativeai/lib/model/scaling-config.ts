/**
 * Generative AI Service Management API
 * OCI Generative AI is a fully managed service that provides a set of state-of-the-art, customizable large language models (LLMs) that cover a wide range of use cases for text generation, summarization, and text embeddings. 

Use the Generative AI service management API to create and manage {@link DedicatedAiCluster}, {@link Endpoint}, {@link Model}, and {@link WorkRequest} in the Generative AI service. For example, create a custom model by fine-tuning an out-of-the-box model using your own data, on a fine-tuning dedicated AI cluster. Then, create a hosting dedicated AI cluster with an endpoint to host your custom model. 

To access your custom model endpoints, or to try the out-of-the-box models to generate text, summarize, and create text embeddings see the [Generative AI Inference API](https://docs.oracle.com/iaas/api/#/en/generative-ai-inference/latest/).

To learn more about the service, see the [Generative AI documentation](https://docs.oracle.com/iaas/Content/generative-ai/home.htm).

 * OpenAPI spec version: 20231130
 * 
 *
 * NOTE: This class is auto generated by OracleSDKGenerator.
 * Do not edit the class manually.
 *
 * Copyright (c) 2020, 2026, Oracle and/or its affiliates.  All rights reserved.
 * This software is dual-licensed to you under the Universal Permissive License (UPL) 1.0 as shown at https://oss.oracle.com/licenses/upl or Apache License 2.0 as shown at http://www.apache.org/licenses/LICENSE-2.0. You may choose either license.
 */

import * as model from "../model";
import common = require("oci-common");

/**
 * The auto scaling configuration for the Hosted Application.
 * Defines the minimum and maximum number of replicas.
 * When unspecified, the service applies service-defined default scaling values.
 *
 */
export interface ScalingConfig {
  /**
   * Minimum number of replicas to keep running. Note: Numbers greater than Number.MAX_SAFE_INTEGER will result in rounding issues.
   */
  "minReplica"?: number;
  /**
   * Maximum number of replicas allowed. Note: Numbers greater than Number.MAX_SAFE_INTEGER will result in rounding issues.
   */
  "maxReplica"?: number;
  /**
   * scaling type for application.
   */
  "scalingType": ScalingConfig.ScalingType;
  /**
   * Scale up if average CPU utilization exceeds this threshold. Note: Numbers greater than Number.MAX_SAFE_INTEGER will result in rounding issues.
   */
  "targetCpuThreshold"?: number;
  /**
   * Scale up if average memory utilization exceeds this threshold. Note: Numbers greater than Number.MAX_SAFE_INTEGER will result in rounding issues.
   */
  "targetMemoryThreshold"?: number;
  /**
   * number of simultaneous requests that can be processed by each replica. Note: Numbers greater than Number.MAX_SAFE_INTEGER will result in rounding issues.
   */
  "targetConcurrencyThreshold"?: number;
  /**
   * requests-per-second per replica of an application. Note: Numbers greater than Number.MAX_SAFE_INTEGER will result in rounding issues.
   */
  "targetRpsThreshold"?: number;
}

export namespace ScalingConfig {
  export enum ScalingType {
    Cpu = "CPU",
    Memory = "MEMORY",
    Concurrency = "CONCURRENCY",
    RequestsPerSecond = "REQUESTS_PER_SECOND",
    /**
     * This value is used if a service returns a value for this enum that is not recognized by this
     * version of the SDK.
     */
    UnknownValue = "UNKNOWN_VALUE"
  }

  export function getJsonObj(obj: ScalingConfig): object {
    const jsonObj = { ...obj, ...{} };

    return jsonObj;
  }
  export function getDeserializedJsonObj(obj: ScalingConfig): object {
    const jsonObj = { ...obj, ...{} };

    return jsonObj;
  }
}
